kind: Service
apiVersion: v1
metadata:
  name: {{OPENSHIFT_APP_NAME}}
  labels:
    app: {{OPENSHIFT_APP_NAME}}
    app.kubernetes.io/part-of: {{OPENSHIFT_APP_NAME}}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app: {{OPENSHIFT_APP_NAME}}